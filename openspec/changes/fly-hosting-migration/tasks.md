## 1. PictureService: revive upload, add photo-URL issuance

- [x] 1.1 In `NinjagoScanner.PictureService/Protos/picture_service.proto`, add a client-streaming `UploadPhoto` RPC (metadata message with original file name, then byte-chunk messages) and remove `AnalyzePhoto`/`AnalyzePhotoRequest`/`AnalyzePhotoResponse`.
- [x] 1.2 Add a `GetPhotoDownloadUrl`-style RPC (single request/response) returning a short-lived presigned GET URL for a given photo ID.
- [x] 1.3 Implement `UploadPhoto`: validate file extension (jpg/jpeg/png/bmp/webp) and non-empty content, generate a photo ID, write bytes to S3 via `PhotoStore` (new write path), create the sidecar record, trigger Gemini analysis, return the resulting `CardEntry`.
- [x] 1.4 Implement the download-URL RPC using the same S3 client `PhotoStore` already holds.
- [x] 1.5 Delete `IUploadUrlIssuer`/`S3UploadUrlIssuer`-equivalent code once nothing references it (it currently lives in `Web.Bff`, retired in section 3).
- [x] 1.6 Update/port `NinjagoScanner.PictureService.Tests` for the new `UploadPhoto` and download-URL RPCs; remove tests for the deleted `AnalyzePhoto`.

## 2. AWS: PictureService's own IAM identity

- [x] 2.1 Add a new Terraform module (e.g. `infra/modules/iam-user/`) provisioning an IAM user + scoped policy: `s3:GetObject`/`PutObject`/`DeleteObject`/`ListBucket` on the photo bucket's `photos/*` prefix, `dynamodb:GetItem`/`PutItem`/`DeleteItem`/`Scan` on the sidecar table.
- [x] 2.2 Wire the module into `infra/environments/prod/main.tf` alongside `photo_storage`/`sidecar_table`.
- [x] 2.3 Generate the access key via Terraform (or `aws iam create-access-key` once, out of band, matching the Gemini-secret pattern of not committing real credentials) and record how to set it as a Fly secret in `infra/README.md`. Applied against the real AWS account (profile `terraform`): `ninjago-scanner-picture-service` IAM user + scoped inline policy + access key created (3 added, 2 changed — the deploy/plan role policies — 0 destroyed). Access key ID `AKIAY5GAKGISAHPKTSW4`; secret retrievable via `terraform output -raw picture_service_secret_access_key` until it's set as a Fly secret (task 4.4, blocked on `flyctl`).

## 3. NinjagoScanner.Web: the merged project

- [x] 3.1 Create `NinjagoScanner.Web` project (Blazor Server, Interactive Server render mode), add it to `NinjagoScanner.slnx`.
- [x] 3.2 Port `NinjagoScanner.Web.Bff/Services/CardCatalogService.cs` and `Services/PictureServiceClient.cs` into the new project largely as-is (gRPC-calling logic unchanged). Download URLs now come from PictureService's `GetPhotoDownloadUrl` RPC instead of an in-process `IUploadUrlIssuer` (Web no longer holds AWS credentials — see design.md Decision 3).
- [x] 3.3 Port `NinjagoScanner.Web.Client`'s pages (`Collection.razor`, `Gallery.razor`, `Review.razor`, `Upload.razor`, `Overview`/home, `About`) into the new project, rewriting each page's data-fetching from `HttpClient`/JSON calls to direct `CardCatalogService` calls. **Note**: `Table.razor` did not exist in `NinjagoScanner.Web.Client` (only CLAUDE.md's stale architecture description and this task list mention it) — nothing to port; flagged for tasks.md/CLAUDE.md cleanup.
- [x] 3.4 Rewrite the upload page to stream `IBrowserFile` bytes to PictureService via the new `UploadPhoto` client-streaming RPC (replacing the old `POST /uploads` → PUT-to-S3 → `POST /uploads/{id}/confirm` sequence), keeping the existing file-picker/camera-hint markup and the upload-in-progress button-disable behavior.
- [x] 3.5 Wire photo display (gallery/review/collection pages) to call the new download-URL RPC per photo and render the returned S3 URL directly as the image source.
- [x] 3.6 Port `NinjagoScanner.Web.Client`'s `wwwroot/appsettings.json`-equivalent configuration (service addresses, max upload size) into the new project's config, reusing the existing resolution precedence (`WebConfig.cs`, ported from `BffConfig.cs`).
- [x] 3.7 Port relevant tests from `NinjagoScanner.Web.Bff.Tests` and `NinjagoScanner.Web.Client.Tests` into `NinjagoScanner.Web.Tests` (46 tests, all passing).
- [x] 3.8 Retire `NinjagoScanner.Web.Client`, `NinjagoScanner.Web.Bff`, `NinjagoScanner.Web.Shared`, and their `.Tests` projects — remove from `NinjagoScanner.slnx` and delete the directories. Also updated `.vscode/launch.json`/`tasks.json` (Web.Bff → Web, dropped the WASM watch-client task). Full solution builds clean and all 133 tests pass.

## 4. Containerize and configure Fly.io

- [x] 4.1 Write `NinjagoScanner.Web/Dockerfile` (same build-context pattern as the existing two Dockerfiles). Verified: `docker build` succeeds and the resulting image starts and serves HTTP 200 on port 8080.
- [x] 4.2 Add `fly.toml` to `NinjagoScanner.Web`, `NinjagoScanner.CatalogService`, `NinjagoScanner.PictureService`, each declaring the app name (`ninjago-scanner-web`/`-catalog-service`/`-picture-service`), the gRPC port + separate health-check port for the two internal services, and public-vs-private reachability (only `web` gets a public IP). Config only — not verified against a real Fly deploy (flyctl isn't installed in this environment); see 4.7. **Corrected during 4.6**: all three `fly.toml`'s `dockerfile` path was wrong — flyctl resolves it relative to the `fly.toml`'s own directory, not the build context root (despite the header comment's assumption); changed `"NinjagoScanner.X/Dockerfile"` to `"Dockerfile"` in each. Also trimmed `.dockerignore` (added `infra/`, `.venv/`, `.claude/`, `CardInfos_Unsorted/`) — build context was 1.5 GB, almost entirely Terraform provider caches under `infra/.terraform`.
- [x] 4.3 `flyctl apps create` for all three apps in the same Fly org/region. Created `ninjago-scanner-web`, `ninjago-scanner-catalog-service`, `ninjago-scanner-picture-service` in org `personal`.
- [x] 4.4 Set Fly secrets: Gemini API key/model and the new AWS IAM user's access key/secret on `picture-service`; any config `web`/`catalog-service` need. Set via `flyctl secrets import --stage`: `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `Storage__PhotosBucketName`, `Storage__SidecarTableName`, `Gemini__ApiKey` on `ninjago-scanner-picture-service` (`Gemini__Model` omitted, defaults to `gemini-2.5-flash`). `web`/`catalog-service` need no secrets — their config is plain env vars already in `fly.toml`. **Also added** `AWS__Region = "eu-central-1"` as a plain env var (task 4.6 found `PictureService`'s `AddDefaultAWSOptions(builder.Configuration.GetAWSOptions())` needs an explicit region — it doesn't fall back to `AWS_REGION`/instance metadata the way the raw AWS SDK does).
- [x] 4.5 Confirm all three apps join the same Fly private network and resolve each other via `*.internal` DNS. Verified via `flyctl ssh console` from `ninjago-scanner-web`: `getent hosts` resolved both `ninjago-scanner-catalog-service.internal` and `ninjago-scanner-picture-service.internal` to `fdaa:...` 6PN addresses, and a raw `/dev/tcp` connect to port 8080 on `catalog-service` succeeded.
- [x] 4.6 First manual `flyctl deploy` for each app; verify `web` → `catalog-service`/`picture-service` gRPC calls succeed over 6PN, verify upload and photo display end-to-end. Deployed catalog-service → picture-service → web, in that order. Fixed two real bugs surfaced only by a production-scale deploy (see 4.2, 4.4 notes above: dockerfile path, AWS region). After both fixes, `/` (Overview), `/gallery`, `/collection`, `/upload` all render successfully via gRPC over 6PN, confirming Katalog-/PictureService reachability and photo-download-URL issuance work end-to-end. **Found but not fixed** (out of scope for this hosting change): `/review` times out — `CardCatalogService.GetReviewGroupsAsync` calls `ToCardListItemsAsync` for every card entry in the whole catalog (not scoped to one series/card like Gallery/Collection are), and each photo's download URL is fetched via a sequential RPC that opens a fresh `GrpcChannel` per call (`CardCatalogService.cs:442-451`, `534-543`). With ~2,500+ real photos this is thousands of sequential round trips — invisible with the small local test dataset, but a real perf bug at production scale. Flagged for a separate follow-up change, not fixed here.
- [x] 4.7 Confirm Fly's health checks correctly probe the separate health-check port (not the gRPC port) for `catalog-service`/`picture-service` — adjust `fly.toml`'s check config if Fly's behavior differs from the ECS-era assumption (see design.md Decision 6). Confirmed via `flyctl status`: both `catalog-service` and `picture-service` show `1 total, 1 passing` against the port-8082 HTTP check; no adjustment needed.

## 5. CI/CD

- [x] 5.1 Add `.github/workflows/deploy-web.yml`, `deploy-catalog-service.yml`, `deploy-picture-service.yml` — path-filtered on push to `main`, using `flyctl deploy --config <project>/fly.toml` (via `superfly/flyctl-actions` or a plain `flyctl` install step) and a `FLY_API_TOKEN` repo/environment secret. Not run — needs a real `FLY_API_TOKEN` and Fly apps to exist first (see 4.3/4.4).
- [x] 5.2 Confirm `.github/workflows/ci.yml` builds/tests the new `NinjagoScanner.Web` project and no longer references the retired `Web.Client`/`Web.Bff`/`Web.Shared` projects. `ci.yml` builds/tests `NinjagoScanner.slnx` generically (no per-project references), and the slnx now lists `NinjagoScanner.Web`/`NinjagoScanner.Web.Tests` instead of the retired projects — confirmed via `grep` that no other workflow references the retired project names.

## 6. Documentation

- [x] 6.1 Update `infra/README.md`'s layout section to include the new IAM-user module and describe Fly.io as the compute host (linking out to each project's `fly.toml` rather than duplicating Fly-specific detail in Terraform docs). Also documents reading the IAM user's access key from Terraform output and setting it (plus Gemini/storage config) as Fly secrets (task 2.3's documentation half).
- [x] 6.2 Update `CLAUDE.md`'s architecture section to describe `NinjagoScanner.Web` (Blazor Server, direct gRPC calls) and Fly.io hosting. The file turned out to be stale in a deeper way than expected — it still described PictureService storing sidecars as local JSON files and Web resolving a shared `cardFotos/` directory, both predating even the `cloud-hosting-migration` S3/DynamoDB switch, plus a `/table` route that was never built. Corrected all of it to match the current S3/DynamoDB/gRPC/Fly architecture.
