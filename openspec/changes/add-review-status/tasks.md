## 1. Proto contract

- [x] 1.1 In `NinjagoScanner.PictureService/Protos/picture_service.proto`, rename `CardEntry.status` (field 2) to `analysis_status` and `UpdateSidecarRequest.status` (field 3) to `analysis_status`, keeping the existing field numbers.
- [x] 1.2 In the same file, add `string review_status` to `CardEntry` and to `UpdateSidecarRequest`, each with a fresh, unused field number.
- [x] 1.3 Apply the identical renames/additions to `NinjagoScanner.Web/Protos/picture_service.proto` so both copies stay in lockstep.
- [x] 1.4 Rebuild both projects to regenerate the gRPC/protobuf C# code and confirm it compiles.

## 2. PictureService models and analysis logic

- [x] 2.1 In `ScannerModels.cs`, rename `CardAnalysisResult.Status` to `AnalysisStatus`, and add a new `ReviewStatuses` constants class (`Unreviewed`, `Verified`, `Incorrect`) plus a `ReviewStatus` property on `CardAnalysisResult` defaulting to `ReviewStatuses.Unreviewed`. Do NOT rename `GeminiCardPayload.Status` or change the Gemini prompt text — that DTO models Gemini's own raw response schema and is a separate concern; `GeminiApiService.NormalizeStatus` continues to take `payload.Status` (Gemini's raw opinion) as input and produce the (now renamed) `AnalysisStatus` as output. (`CardAnalysisResult` was also converted from a class to a record so the Scan loop can carry forward an existing `ReviewStatus` across rescans via `with`.)
- [x] 2.2 In `SidecarStore.cs`, rename `SidecarRecord.Status` to `AnalysisStatus` and add `SidecarRecord.ReviewStatus` (nullable string, no default-value assumptions since the type is a lenient reader). Make `ReadRecordAsync` backward-compatible: if the deserialized record's `AnalysisStatus` is null/empty but the raw JSON contains a legacy `status` key, populate `AnalysisStatus` from that legacy value in memory (do not rewrite the file here — that is `MigrateSidecars`' job).
- [x] 2.3 In `GeminiApiService.cs`, update `NormalizeStatus` and all call sites/property assignments to use `AnalysisStatus` instead of `Status` (except the `payload.Status` input parameter, which stays as-is per 2.1); keep the derivation logic (including the `Confidence < 0.65` gate) unchanged.
- [x] 2.4 In `Services/PictureScannerGrpcService.cs`, update all `Status = ...` literals/assignments to `AnalysisStatus`, and ensure freshly created sidecar records (new scans, and the `"pending"` placeholder for missing sidecars) set `ReviewStatus = ReviewStatuses.Unreviewed`. Also added: the Scan loop now preserves an existing sidecar's `ReviewStatus` across rescans (reads it before overwriting) instead of resetting it, per the "rescanning does not change ReviewStatus" spec requirement.
- [x] 2.5 Ensure `UpdateSidecar` handling persists an incoming `review_status` from the request into the sidecar record without altering it when the request does not intend to change it, and never derives it from `analysis_status` or `confidence`.

## 3. Sidecar migration RPC

- [x] 3.1 In both `picture_service.proto` files, add `MigrateSidecarsRequest` (optional `card_photos_directory` override, same pattern as `ListCardsRequest`) and `MigrateSidecarsResponse` (e.g. `total_files`, `migrated`, `already_current`, `errors` counts) messages, and a `MigrateSidecars` RPC on `CardPictureService`.
- [x] 3.2 Implement `PictureScannerGrpcService.MigrateSidecars`: resolve the directory via the same `ResolveDirectory` helper used by other RPCs, enumerate sidecar `*.json` files, for each one detect a legacy `status` key with no `AnalysisStatus` key, rewrite it via `SidecarStore.WriteRecordAsync` using the migrated record, and skip files already on the new key. Return counts in the response.
- [x] 3.3 Verify idempotency: running the RPC twice in a row against the same directory produces zero additional rewrites on the second run. Verified live: ran PictureService against a throwaway directory with a hand-crafted legacy sidecar (`{"Status": "ok", ...}`, no `AnalysisStatus` key); first `MigrateSidecars` call returned `Migrated=1`, second returned `Migrated=0, AlreadyCurrent=1`. Confirmed on disk the file now serializes with an `AnalysisStatus` key and no `Status` key.

## 4. Web mapping and models

- [x] 4.1 In `NinjagoScanner.Web/Services/CardCatalogService.cs`, update all proto <-> model mapping (`ToCardListItem`, `ToCollectionSidecar`, and the `UpdateSidecar` request builder) to read/write `AnalysisStatus` and `ReviewStatus` instead of `Status`.
- [x] 4.2 In `Models/CardListItem.cs`, rename `Status` to `AnalysisStatus` and add a `ReviewStatus` property. (Also added a `ReviewStatuses` constants class here, mirroring PictureService's, since the Web project can't reach PictureService's `internal` one.)
- [x] 4.3 In `Models/CollectionCardDetails.cs`, rename `Status` to `AnalysisStatus` and add `ReviewStatus` on both `CollectionCardSidecarData` and `CollectionCardSidecarUpdate`.

## 5. Collection edit form (Web UI)

- [x] 5.1 In `Components/Pages/Collection.razor`, replace the editable `<input @bind="sidecarDraft.Status" />` with a read-only display of `AnalysisStatus` (plain text next to the "Zuletzt gescannt" info), removing it from the fields that get submitted for edit.
- [x] 5.2 Add a `ReviewStatus` `<select>` control to the edit form grid with the three fixed options (`unreviewed`, `verified`, `incorrect`), bound to `sidecarDraft.ReviewStatus`.
- [x] 5.3 Update `SidecarDraft`, `BuildDraft`, and `SaveSelectedSidecarAsync` so saving the form sends the current `ReviewStatus` value. Note: `AnalysisStatus` is still round-tripped unchanged in the update request (it's not exposed as an editable control, so `sidecarDraft.AnalysisStatus` always holds the value `BuildDraft` populated it with) since the proto/gRPC fields have no presence tracking and always reflect the full form state, matching the existing pattern for every other field.
- [x] 5.4 Verify manually: editing and saving any other field (name, number, set, rarity, etc.) without touching the review control leaves `ReviewStatus` unchanged. Verified at the gRPC/`UpdateSidecar` level directly (not through the browser, see 7.2): set `ReviewStatus=verified`, then sent a second `UpdateSidecar` request changing only `CardName` while round-tripping the existing `ReviewStatus` — `ReviewStatus` remained `verified` after the second save. This is the exact mechanism the Collection.razor form relies on, since `SaveSelectedSidecarAsync` always sends `sidecarDraft.ReviewStatus` (5.3).

## 6. List views and filtering (Web UI)

- [x] 6.1 In `Components/Pages/Home.razor`, rename all `card.Status`/`Status` references (badges, grouping, existing filter, `GetStatusLabel`/`GetStatusClass`) to use `AnalysisStatus`.
- [x] 6.2 In `Components/Pages/Home.razor`, add a second, independent filter for `ReviewStatus` (`selectedReviewStatus`, `AvailableReviewStatuses`, `GetReviewStatusLabel`/`GetReviewStatusClass` helpers reusing existing badge classes per design.md), following the same pattern as the `AnalysisStatus` filter. Also added a second status badge on each card tile for visibility.
- [x] 6.3 Apply the equivalent renames and new `ReviewStatus` filter to `Components/Pages/CardsTable.razor` (this page had no dedicated status filter dropdown before, only search+grouping, so a new "Review-Status" filter dropdown and table column were added).

## 7. Verification

- [x] 7.1 Build the full solution (`NinjagoScanner.slnx`) and confirm no remaining references to the old `Status` field name anywhere in PictureService or Web source, outside of `GeminiCardPayload`. Solution builds with 0 warnings/errors; grep sweep confirms remaining `Status` matches are only `GeminiCardPayload.Status`, UI label text, and the unrelated `Grpc.Core.Status` type.
- [ ] 7.2 Run a manual end-to-end check **through the actual Blazor UI in a browser**: scan a new photo (confirm `AnalysisStatus` populates and `ReviewStatus` defaults to `unreviewed`), mark it `verified` via the UI, filter the list by `verified`, then edit an unrelated field and confirm `ReviewStatus` stays `verified`. NOT completed by this implementation session — requires a configured `GEMINI_API_KEY` and manual browser interaction, neither of which was available here. The underlying mechanics (backward-compatible read, migration, `ReviewStatus` persistence/independence) were verified directly at the gRPC layer instead — see 3.3, 5.4, 7.3 — but the actual Web UI and a live Gemini scan were not exercised. Recommend running this manually before considering the change fully done.
- [x] 7.3 Confirm a sidecar file with the old `status` JSON key is read correctly into `AnalysisStatus` without a rescan (backward-compatible read from task 2.2). Verified live: `ListCards` against a directory containing only a legacy-keyed sidecar (`{"Status": "ok", ...}`) returned `AnalysisStatus='ok'` before any migration ran.
- [x] 7.4 Run `MigrateSidecars` against a directory containing such a legacy file; confirm the file is rewritten to use the `AnalysisStatus` key with the same value it had, then run it again and confirm no further changes occur. Verified live on synthetic data, see 3.3. Also run for real against the actual `cardFotos` directory (3102 sidecar files, all pre-existing on the legacy `status` key): `TotalFiles=3102 Migrated=3102 AlreadyCurrent=0 Errors=0`. Spot-checked a file before/after against a pre-migration backup — only the `Status`→`AnalysisStatus` key rename changed, every other field byte-identical. Confirmed zero files retain the legacy `"Status"` key afterward.
- [ ] 7.5 Confirm `GeminiCardPayload`/the prompt were left untouched by running a real scan and verifying `AnalysisStatus` is still classified correctly (`ok`/`uncertain`/`failed`) from Gemini's response. NOT completed — requires a configured `GEMINI_API_KEY` to call the real Gemini API, not available in this session. Static verification only: `GeminiCardPayload.Status` and the prompt text in `GeminiApiService.cs` were left byte-for-byte unchanged (confirmed via diff/grep during 7.1); `NormalizeStatus(payload.Status, payload.Confidence)` still reads that field name unchanged. Recommend running one real scan to fully confirm before considering the change done.
